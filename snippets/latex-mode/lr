# -*- mode: snippet -*-
# name: Left Right
# key: lr
# --
\left{${1:(}}$0\right{${1:$(let ((text yas-text)) (-each sp-pair-list (lambda (cs) (setq text (replace-regexp-in-string (regexp-quote (car cs)) (cdr cs) text)))) text)}}